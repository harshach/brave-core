diff --git a/chrome/browser/ui/browser_live_tab_context.cc b/chrome/browser/ui/browser_live_tab_context.cc
index d80d6550e73ccd088505e8dd00350cf5002caea9..b239a80c9d7ba98ebab108dbdbebbdddf800540d 100644
--- a/chrome/browser/ui/browser_live_tab_context.cc
+++ b/chrome/browser/ui/browser_live_tab_context.cc
@@ -120,6 +120,7 @@ BrowserLiveTabContext::~BrowserLiveTabContext() {
 }
 
 void BrowserLiveTabContext::ShowBrowserWindow() {
+  BraveFinishOriginSpaceWindowRestore(*browser_);
   base_window_->Show();
 }
 
@@ -161,6 +162,7 @@ sessions::LiveTab* BrowserLiveTabContext::GetActiveLiveTab() const {
 std::map<std::string, std::string> BrowserLiveTabContext::GetExtraDataForTab(
     int index) const {
   std::map<std::string, std::string> extra_data;
+  MaybePopulateTreeTabExtraData(*browser_, index, extra_data);
 
   glic::PopulateGlicExtraData(tab_strip_model_->GetTabAtIndex(index),
                               &extra_data);
@@ -189,7 +191,9 @@ BrowserLiveTabContext::GetExtraDataForWindow() const {
           focused_group->ToString();
     }
   }
 
+  MaybePopulateOriginSpaceWindowExtraData(*browser_, data);
+
   return data;
 }
 
@@ -522,7 +526,8 @@ sessions::LiveTabContext* BrowserLiveTabContext::Create(
   }
 
   Browser* browser = CreateBrowserWindow(std::move(*create_params))
                          ->GetBrowserForMigrationOnly();
+  BraveBeginOriginSpaceWindowRestore(*browser, extra_data);
 
   return browser->GetFeatures().live_tab_context();
 }
