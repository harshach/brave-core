diff --git a/chrome/browser/ui/download/download_bubble_info_utils.cc b/chrome/browser/ui/download/download_bubble_info_utils.cc
index 29eb774f5c61096e96cf15782e53efa16e072b65..2ae39c160d11ad07ca5e8b16335e1cdd1136df1b 100644
--- a/chrome/browser/ui/download/download_bubble_info_utils.cc
+++ b/chrome/browser/ui/download/download_bubble_info_utils.cc
@@ -374,6 +374,7 @@ std::vector<DownloadBubbleQuickAction> QuickActionsForDownload(
         &(features::IsRoundedIconsEnabled()
               ? vector_icons::kOpenInNewFlippableIcon
               : vector_icons::kLaunchChromeRefreshOldIcon));
+    AddDeleteLocalFileQuickAction(actions);
   }
 
   return actions;
@@ -452,6 +453,7 @@ DownloadBubbleProgressBar ProgressBarForDownload(const DownloadUIModel& model) {
       break;
   }
 
+  if (model.GetState() == DownloadItem::IN_PROGRESS && model.AllDataSaved()) return DownloadBubbleProgressBar::LoopingProgressBar();
   return model.GetState() == DownloadItem::IN_PROGRESS
              ? DownloadBubbleProgressBar::ProgressBar()
              : DownloadBubbleProgressBar::NoProgressBar();
