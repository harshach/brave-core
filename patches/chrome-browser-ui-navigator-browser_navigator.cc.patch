diff --git a/chrome/browser/ui/navigator/browser_navigator.cc b/chrome/browser/ui/navigator/browser_navigator.cc
index f8d51faf9020f0fd045b0e04718d12ff609c96f7..00f9c03175b7d0904f96cdba9fda5f2523379043 100644
--- a/chrome/browser/ui/navigator/browser_navigator.cc
+++ b/chrome/browser/ui/navigator/browser_navigator.cc
@@ -116,6 +116,7 @@ struct AdditionalParams {
 // popups. TYPE_APP will open a new tab if the browser was launched from a
 // template, otherwise only if the URL is within the app scope.
 bool WindowCanOpenTabs(const NavigateParams& params) {
+  BRAVE_WINDOW_CAN_OPEN_TABS
   if (!params.browser) {
     return false;
   }
@@ -191,6 +192,7 @@ bool ShouldForceCaptivePortalSigninIntoCurrentTab(const NavigateParams& params,
 // Returns true on success. Otherwise, if changing params leads the browser
 // into an erroneous state, returns false.
 bool AdjustNavigateParamsForURL(NavigateParams* params) {
+  UpdateParams(params);
   // Check for some chrome:// pages which we always want to open in a
   // non-incognito window. Note that even though a ChromeOS guest session is
   // technically an incognito window, these URLs are allowed.
@@ -485,7 +487,11 @@ std::unique_ptr<content::WebContents> CreateTargetContents(
   scoped_refptr<content::SiteInstance> initial_site_instance_for_new_contents =
       params.opener ? params.opener->GetSiteInstance()
                     : tab_util::GetSiteInstanceForNewTab(
-                          params.browser->GetProfile(), url);
+                          params.browser->GetProfile(), url
+#if BUILDFLAG(ENABLE_CONTAINERS)
+                          , GetStoragePartitionConfigToInherit(params)
+#endif  // BUILDFLAG(ENABLE_CONTAINERS)
+);
 
   WebContents::CreateParams create_params(
       params.browser->GetProfile(), initial_site_instance_for_new_contents);
@@ -877,6 +883,13 @@ base::WeakPtr<content::NavigationHandle> NavigateImpl(
          params->user_gesture)) {
       tab_to_insert->set_opener(
           tabs::TabInterface::MaybeGetFromContents(params->source_contents));
+      // Tree tabs should only inherit hierarchy from a genuine page-created
+      // navigation. Browser commands such as Ctrl+T, Quick Open, and typed
+      // address-bar navigations receive the active tab as source context too,
+      // but that is not a parent/child relationship.
+      if (params->navigation_initiated_from_sync || user_initiated) {
+        tab_to_insert->set_opener_was_set_for_empty_new_tab();
+      }
     }
   }
 
@@ -894,6 +907,11 @@ base::WeakPtr<content::NavigationHandle> NavigateImpl(
            params->user_gesture)) {
         tab_to_insert->set_opener(
             tabs::TabInterface::MaybeGetFromContents(params->source_contents));
+        // See the contents-to-insert path above. Web-triggerable link opens
+        // keep their opener and therefore remain nested under their source.
+        if (params->navigation_initiated_from_sync || user_initiated) {
+          tab_to_insert->set_opener_was_set_for_empty_new_tab();
+        }
       }
       contents_to_navigate_or_insert = tab_to_insert->GetContents();
 
